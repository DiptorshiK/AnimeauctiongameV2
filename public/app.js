const socket=io();let state=null,myId=null,creds=null,anime=null;
const A={naruto:["🍥","Naruto + Naruto Shippuden","https://cdn.myanimelist.net/images/anime/13/17405.jpg"],jjk:["👁️","Jujutsu Kaisen","https://cdn.myanimelist.net/images/anime/1171/109222.jpg"],aot:["🧱","Attack on Titan","https://cdn.myanimelist.net/images/anime/10/47347.jpg"],demon_slayer:["⚔️","Demon Slayer","https://cdn.myanimelist.net/images/anime/1286/99889.jpg"],death_note:["📓","Death Note","https://cdn.myanimelist.net/images/anime/9/9453.jpg"]};
const $=x=>document.getElementById(x), esc=s=>String(s).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));
function toast(x){$("toast").textContent=x;$("toast").style.display="block";clearTimeout(window.to);window.to=setTimeout(()=>$("toast").style.display="none",2500)}
function show(x){document.querySelectorAll(".view").forEach(v=>v.classList.add("hidden"));$(x).classList.remove("hidden")}
function cards(){ $("animeGrid").innerHTML=Object.entries(A).map(([k,v])=>`<div class="animeCard"><div><div class="animeArt" style="background-image:url('${v[2]}')"><div class="animeIcon">${v[0]}</div></div><h3>${v[1]}</h3><p>PRIVATE MULTIPLAYER ROOM</p></div><button onclick="create('${k}')">CREATE ROOM →</button></div>`).join("")}
cards();
function create(k){anime=k;const name=prompt("Your name");if(!name)return;const password=prompt("Room password (optional)","");const budget=prompt("Starting budget","100")||100;socket.emit("createRoom",{anime:k,name,budget,password})}
function join(){const code=$("joinCode").value.trim(),name=$("joinName").value.trim(),password=$("joinPass").value;if(!code||!name)return toast("Enter room code and your name.");socket.emit("joinRoom",{code,name,password})}
function host(action,payload={}){if(!creds?.hostToken)return;socket.emit("hostAction",{code:state.code,hostToken:creds.hostToken,action,payload})}
function bid(){socket.emit("bid",{code:state.code,token:creds.token})}
function copyInvite(){navigator.clipboard.writeText($("invite").value);toast("Invite link copied")}
socket.on("connect",()=>{myId=socket.id;$("topStatus").textContent="CONNECTED";const c=localStorage.getItem("aa-creds");if(c){try{const x=JSON.parse(c);creds=x;socket.emit("rejoin",x)}catch{}}});
socket.on("credentials",c=>{creds=c;localStorage.setItem("aa-creds",JSON.stringify(c));history.replaceState({},'',`?room=${c.code}`)});
socket.on("errorMsg",toast);socket.on("kicked",()=>{localStorage.removeItem("aa-creds");toast("You were removed from the room.");setTimeout(()=>location.href=location.pathname,1000)});
socket.on("state",s=>{state=s;render()});
function render(){anime=state.anime;if(state.finished){show("finish");finish();return}if(!state.started){show("lobby");lobby();return}show("auction");auction()}
function lobby(){$("lobbyAnime").textContent=A[state.anime][1].toUpperCase();$("roomCode").textContent=state.code;$("invite").value=location.origin+location.pathname+`?room=${state.code}`;$("count").textContent=`(${state.players.length})`;$("start").style.display=creds?.hostToken?"block":"none";$("lobbySub").textContent=state.locked?"Room locked":"Waiting for players…";$("lobbyPlayers").innerHTML=state.players.map(p=>`<div class="person"><b>${esc(p.name)} ${p.id===state.hostId?"👑":""}</b><span class="${p.online?"online":""}">${p.online?"● ONLINE":"○ OFFLINE"} · 💰 ${p.budget}</span>${creds?.hostToken&&p.id!==state.hostId?`<button class="adminBtn" onclick="kick('${p.id}')">REMOVE</button>`:""}</div>`).join("");if(creds?.hostToken){$("settings").innerHTML=`<h3>HOST SETTINGS</h3><label>STARTING BUDGET</label><input id="sb" value="${state.budget}"><label>BID INCREMENT</label><input id="si" value="${state.increment}"><label>TIMER (SECONDS)</label><input id="st" value="${state.timerSeconds}"><label>ROOM PASSWORD</label><input id="sp" type="password"><button onclick="saveSettings()">SAVE SETTINGS</button><p style="color:#81899b;font-size:11px">Host controls unlock automatically once the auction starts.</p>`}else $("settings").innerHTML="<h3>WAITING FOR HOST</h3><p style='color:#8e96a9;font-size:12px;line-height:1.6'>The host controls the auction. Keep this page open.</p>"}
function saveSettings(){host("settings",{budget:$("sb").value,increment:$("si").value,timer:$("st").value,password:$("sp").value})}
function kick(id){host("kick",{id})}
function auction(){const me=state.players.find(p=>p.id===myId);$("auctionAnime").textContent=A[state.anime][1];$("progress").textContent=`${Math.min(state.index+1,state.total)} / ${state.total}`;$("roomSmall").textContent=state.code;$("wallet").textContent=me?`💰 ${me.budget}`:"—";$("sidePlayers").innerHTML=state.players.map(p=>`<div class="playerMini"><b>${esc(p.name)}${p.id===state.hostId?" 👑":""}</b><span>${p.budget}</span></div>`).join("");$("history").innerHTML=state.history.slice().reverse().map(x=>`<div class="sale"><b>${esc(x.name)}</b><span>${x.bidderName==="Unsold"?"UNSOLD":"💰 "+x.bid+" · "+esc(x.bidderName)}</span></div>`).join("");$("myRoster").innerHTML=me?.roster.length?me.roster.map(x=>`<div class="rosterItem"><b>${esc(x.name)}</b><span>Rating ${x.rating} · Paid ${x.price}</span></div>`).join(""):"<p style='color:#8e96a9;font-size:12px'>No characters yet.</p>";if(!state.current)return;$("charName").textContent=state.current.name;$("rating").textContent=`POWER ${state.current.rating}/10`;$("rarity").textContent=state.current.rating>=9?"LEGENDARY":state.current.rating>=8?"ELITE":"RARE";$("base").textContent=state.current.base;$("timer").textContent=state.current.timeLeft;$("bid").textContent=state.current.bid;$("bidder").textContent=state.current.bidderName?`Leading: ${esc(state.current.bidderName)}`:"No bids yet";$("nextBid").textContent=state.current.bid+state.increment;const can=me&&state.current.bid+state.increment<=me.budget&&!state.paused;$("bidBtn").disabled=!can;loadPortrait(state.current.name);if(creds?.hostToken){$("hostBar").innerHTML=`<button class="adminBtn" onclick="host('pause',{value:${!state.paused}})">${state.paused?"RESUME":"PAUSE"}</button><button class="adminBtn" onclick="host('skip')">SKIP / NEXT</button><button class="adminBtn" onclick="host('lock',{value:${!state.locked}})">${state.locked?"UNLOCK":"LOCK ROOM"}</button><button class="adminBtn" onclick="host('end')">END AUCTION</button>`}else $("hostBar").innerHTML=state.paused?"<small>AUCTION PAUSED BY HOST</small>":""}
function loadPortrait(name){const img=$("portrait"),fallback=$("portraitFallback");fallback.textContent=A[state.anime][0];img.classList.remove("loaded");const q=encodeURIComponent(name);img.onload=()=>{$("portraitWrap")?.classList?.add("loaded");img.parentElement.classList.add("loaded")};img.onerror=()=>{img.parentElement.classList.remove("loaded")};img.src=`https://api.jikan.moe/v4/characters?q=${q}&limit=1`;fetch(`https://api.jikan.moe/v4/characters?q=${q}&limit=1`).then(r=>r.json()).then(d=>{const u=d?.data?.[0]?.images?.jpg?.large_image_url||d?.data?.[0]?.images?.jpg?.image_url;if(u){img.src=u;img.parentElement.classList.add("loaded")}}).catch(()=>{})}
function finish(){$("final").innerHTML=`<div class="finalGrid">${state.players.map(p=>`<div class="finalCard"><h3>${esc(p.name)} ${p.id===state.hostId?"👑":""}</h3><p>💰 ${p.budget} remaining<br>${p.roster.length?p.roster.map(x=>`${esc(x.name)} — ${x.price}`).join("<br>"):"No characters won"}</p></div>`).join("")}</div>`}
const q=new URLSearchParams(location.search).get("room");if(q){$("joinCode").value=q.toUpperCase();$("joinName").focus()}
